# Copy to .env and fill in real values (never commit .env)
MTJ_USER=mtj
MTJ_PASS=changeme
# Optional: 32+ char secret for signing session cookies. If unset, a random key
# is generated and persisted to data/session_secret.key on first boot.
# MTJ_SESSION_SECRET=

