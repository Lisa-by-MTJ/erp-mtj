# Runtime data — never commit business data
data/
*.db
*.sqlite
*.sqlite3

# Container image exports (rebuild from Containerfile instead)
*.tar.gz

# Node
node_modules/
package-lock.json

# Environment / secrets
.env
.env.*
!.env.example
# Invoice PDFs served from bind-mounted data dir, never in git

# parsed PDF data (PII: customer names/phones/serials) — keep local, never commit
scripts/do-import/do_parsed.json
